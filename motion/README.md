# API H product film

A 36-second, 1920×1080 Remotion film based on API H's existing visual system and measured evaluation data.

```bash
npm install
npm run studio
npm run render
npm run still
```

Outputs are written to `out/`.
